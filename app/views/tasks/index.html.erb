<div class="container mt-3">
  <h1>My Tasks</h1>
  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task), class:'text-decoration-none'%></li>
    <% end %>
  </ul>
  <p><%= link_to 'Add a new task', new_task_path, class:'text-decoration-none' %></p>
</div>
